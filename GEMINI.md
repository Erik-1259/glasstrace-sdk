# Gemini Instructions

Treat `AGENTS.md` as the authoritative shared repo policy and routing
model.

For this repo:
- `workstream = sdk`
- default to the stricter public-package review bar
- use `docs/agent-reference/sdk-workstream.md` for SDK-specific review
  expectations

Claude-specific orchestration lives in `CLAUDE.md`. Keep Gemini-specific
notes thin and repo-local rather than duplicating shared policy here.
